# Durable notes (2026-04-23)

- User preference: main chat (Telegram and WhatsApp) should use model `openai-codex/gpt-5.2`.
- User preference: cron jobs should generally use `xiaomi/mimo-v2-omni`.
- User approved restarting OpenClaw gateway so main chat picks up the new default model immediately.

## whitepaper.site marketing context
- Target market: B2B (business owner, brand owner, manager).
- Main service: corporate event.
- Location focus: Jakarta and Tangerang.
- Current lead sources: Instagram and word of mouth.
- Closing channel: WhatsApp.
- User interested in autonomous lead and content systems (IG content engine, funnel, and marketing automation).
- Preference: Whitepaper monthly batch content cron should be executed by agent `doni`, with Exel/main only doing oversight checks.
- Decision: monthly batch is split by specialty: Axis does research brief, Doni generates content plan from the brief, Relay schedules to Repliz, Main/Exel does QC.
# Durable notes (2026-04-23)

- User preference: main chat (Telegram and WhatsApp) should use model `openai-codex/gpt-5.2`.
- User preference: cron jobs should generally use `xiaomi/mimo-v2-omni`.
- User approved restarting OpenClaw gateway so main chat picks up the new default model immediately.

## whitepaper.site marketing context
- Target market: B2B (business owner, brand owner, manager).
- Main service: corporate event.
- Location focus: Jakarta and Tangerang.
- Current lead sources: Instagram and word of mouth.
- Closing channel: WhatsApp.
- User interested in autonomous lead and content systems (IG content engine, funnel, and marketing automation).
- Preference: Whitepaper monthly batch content cron should be executed by agent `doni`, with Exel/main only doing oversight checks.
- Decision: monthly batch is split by specialty: Axis does research brief, Doni generates content plan from the brief, Relay schedules to Repliz, Main/Exel does QC.

## OpenClaw VPS routing and exposure
- User wants OpenClaw reachable on the VPS directly, not dependent on Tailscale for primary access.
- Working setup on 2026-04-23: OpenClaw Control is exposed via HTTPS reverse proxy at `https://ocindonesia.my.id/openclaw/`.
- Direct public exposure on port `18789` was closed again after the HTTPS proxy was confirmed working.
- `openclaw-gateway.service` is the healthy primary service; legacy `openclaw.service` had been disabled because it crash-looped on a systemd bus check.
- Tailscale should remain enabled only for backup/admin access, not as the default outbound internet path.
- Root cause of broken outbound app traffic was an active Tailscale exit node, which caused traffic to route via `tailscale0` through table `52` / rule `5270`.
- Safe fix applied: remove the Tailscale exit node while keeping Tailscale running; desired outbound path for Telegram, WhatsApp, OpenAI, and normal internet is `eth0`.
# Durable notes (2026-04-23)

- User preference: main chat (Telegram and WhatsApp) should use model `openai-codex/gpt-5.2`.
- User preference: cron jobs should generally use `xiaomi/mimo-v2-omni`.
- User approved restarting OpenClaw gateway so main chat picks up the new default model immediately.

## whitepaper.site marketing context
- Target market: B2B (business owner, brand owner, manager).
- Main service: corporate event.
- Location focus: Jakarta and Tangerang.
- Current lead sources: Instagram and word of mouth.
- Closing channel: WhatsApp.
- User interested in autonomous lead and content systems (IG content engine, funnel, and marketing automation).
- Preference: Whitepaper monthly batch content cron should be executed by agent `doni`, with Exel/main only doing oversight checks.
- Decision: monthly batch is split by specialty: Axis does research brief, Doni generates content plan from the brief, Relay schedules to Repliz, Main/Exel does QC.
# Durable notes (2026-04-23)

- User preference: main chat (Telegram and WhatsApp) should use model `openai-codex/gpt-5.2`.
- User preference: cron jobs should generally use `xiaomi/mimo-v2-omni`.
- User approved restarting OpenClaw gateway so main chat picks up the new default model immediately.

## whitepaper.site marketing context
- Target market: B2B (business owner, brand owner, manager).
- Main service: corporate event.
- Location focus: Jakarta and Tangerang.
- Current lead sources: Instagram and word of mouth.
- Closing channel: WhatsApp.
- User interested in autonomous lead and content systems (IG content engine, funnel, and marketing automation).
- Preference: Whitepaper monthly batch content cron should be executed by agent `doni`, with Exel/main only doing oversight checks.
- Decision: monthly batch is split by specialty: Axis does research brief, Doni generates content plan from the brief, Relay schedules to Repliz, Main/Exel does QC.

## OpenClaw VPS routing and exposure
- User wants OpenClaw reachable on the VPS directly, not dependent on Tailscale for primary access.
- Working setup on 2026-04-23: OpenClaw Control is exposed via HTTPS reverse proxy at `https://ocindonesia.my.id/openclaw/`.
- Direct public exposure on port `18789` was closed again after the HTTPS proxy was confirmed working.
- `openclaw-gateway.service` is the healthy primary service; legacy `openclaw.service` had been disabled because it crash-looped on a systemd bus check.
- Tailscale should remain enabled only for backup/admin access, not as the default outbound internet path.
- Root cause of broken outbound app traffic was an active Tailscale exit node, which caused traffic to route via `tailscale0` through table `52` / rule `5270`.
- Safe fix applied: remove the Tailscale exit node while keeping Tailscale running; desired outbound path for Telegram, WhatsApp, OpenAI, and normal internet is `eth0`.

## Preferences: role and delegation
- User wants Exel to act primarily as an orchestrator (delegate work to other agents) + QC/review before publish/apply.
- User does not want to specify which agent; Exel should choose the best agent automatically.

## Content ops: Threads (Whitepaper)
- Preferred Threads style: mix of tips + authority + checklist + opinion + clear CTA (comment keyword/DM).
- Scheduling requirement: images must be rolled/unique; user does not want repeated photos across posts (even if crop differs).
- When scheduling image posts, use 2–3 images per post; avoid repeating the same image across the scheduled range.

## Content ops: LinkedIn (Whitepaper)
- Add website link `https://whitepaper.site` in LinkedIn content.
